import React from 'react';
import {
  View,
  Image as RnImage,
  TouchableOpacity,
  Image,
  ActivityIndicator,
} from 'react-native';
import moment, {duration} from 'moment';
import {Text} from '..';
import styles from './ShiftsListTabsStyles';
import {Colors, Fonts, AppStyles, Images} from '../../theme';
import {strings} from '../../constants';

export default function ShiftsListTabsView(props) {
  return (
    <View>
      <Text>aa</Text>
    </View>
  );
}
